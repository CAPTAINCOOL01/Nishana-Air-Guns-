.DS_Store
Thumbs.db
node_modules/
.vercel/
*.zip

.vercel

# Env files — NEVER commit. Razorpay secret + any server keys live here.
.env
.env.local
.env.*.local

# Claude Code local session cache — machine-specific, do not commit.
.claude/

# Bulk-publish user config (prices + provenance attestations) — stays local.
# The .example.json template IS committed as documentation.
scripts/bulk-config.json
